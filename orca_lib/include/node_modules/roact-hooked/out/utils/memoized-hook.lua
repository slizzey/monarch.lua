--[[
-- Orca, a free and open-source Roblox script hub.
-- This script was generated by ci/bundle.lua, and is not intended to be modified.
-- To view the source code, see the 'src' folder on GitHub!
--
-- Author: 0866
-- License: MIT
-- Version: "1.1.1"
-- GitHub: https://github.com/richie0866/orca
--]]

local tF=table.concat({
    "Invalid hook call. Hooks can only be called inside of the body of a function component.",
    "This is usually the result of conflicting versions of roact-hooked.",
    "See https://reactjs.org/link/invalid-hook-call for tips about how to debug and fix this problem."
},"\n")
local tG="Failed to render hook! (Another hooked component is rendering)"
local tH="Failed to render hook! (Another hooked component rendered during this one)"
local tI
local tJ
local function tK(qO)
    local y=tJ==nil
    assert(y,tG)
    tJ=qO
end
local function tL(qO)
    local y=tJ==qO
    assert(y,tH)
    tJ=nil
    tI=nil
end
local function tm()
    return tJ or error(tF,3)
end
local function tj(bG)
    local tJ=tm()
    local b3
    if tI then
        b3=tI.next
    else
        b3=tJ.firstHook
    end
    local tM=b3
    if tM then
        tI=tM
    else
        local b4
        if type(bG)=="function" then
            b4=bG()
        else
            b4=bG
        end
        local F=b4
        local tN={id=tI and tI.id+1 or 0,state=F,baseState=F}
        if not tI then
            tI=tN
            tJ.firstHook=tI
        else
            tI.next=tN
            tI=tI.next
        end
    end
    return tI
end
return {
    renderReady=tK,
    renderDone=tL,
    resolveCurrentComponent=tm,
    memoizedHook=tj
}
