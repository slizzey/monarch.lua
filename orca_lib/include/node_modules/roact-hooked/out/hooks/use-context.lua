--[[
-- Orca, a free and open-source Roblox script hub.
-- This script was generated by ci/bundle.lua, and is not intended to be modified.
-- To view the source code, see the 'src' folder on GitHub!
--
-- Author: 0866
-- License: MIT
-- Version: "1.1.1"
-- GitHub: https://github.com/richie0866/orca
--]]

local a=_G[script]
local tl=a.import(script,script.Parent.Parent,"utils","memoized-hook")
local tj=tl.memoizedHook
local tm=tl.resolveCurrentComponent
local k=a.import(script,script.Parent,"use-effect").useEffect
local aK=a.import(script,script.Parent,"use-state").useState
local function tn(qO)
    return setmetatable({},{__index=qO})
end
local function ee(qp)
    local to=qp
    local Z=tj(function()
        local oj=tn(tm())
        to.Consumer.init(oj)
        return oj.contextEntry
    end)
    local tp=Z.state
    if tp then
        local dv=aK(tp.value)
        local d3=dv[1]
        local m3=dv[2]
        k(function()
            return tp.onUpdate:subscribe(m3)
        end,{})
        return d3
    else
        return to.defaultValue
    end
end
return {useContext=ee}
