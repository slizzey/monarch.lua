--[[
-- Orca, a free and open-source Roblox script hub.
-- This script was generated by ci/bundle.lua, and is not intended to be modified.
-- To view the source code, see the 'src' folder on GitHub!
--
-- Author: 0866
-- License: MIT
-- Version: "1.1.1"
-- GitHub: https://github.com/richie0866/orca
--]]

local a=_G[script]
local tq=a.import(script,script.Parent.Parent,"utils","are-deps-equal").areDepsEqual
local tl=a.import(script,script.Parent.Parent,"utils","memoized-hook")
local tj=tl.memoizedHook
local tm=tl.resolveCurrentComponent
local function tr(eq)
    local Z=tm()
    local ts=Z.effects
    if ts.tail==nil then
        ts.tail=eq
        ts.head=ts.tail
    else
        local aA=ts.tail
        aA.next=eq
        ts.tail=aA.next
    end
    return eq
end
local function k(da,di)
    local tt=tj(nil)
    local tu=tt.state
    if tu~=nil then
        tu=tu.deps
    end
    local tv=tu
    if di and tq(di,tv) then
        return nil
    end
    tt.state=tr({id=tt.id,callback=da,deps=di})
end
return {useEffect=k}
