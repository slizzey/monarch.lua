--[[
-- Orca, a free and open-source Roblox script hub.
-- This script was generated by ci/bundle.lua, and is not intended to be modified.
-- To view the source code, see the 'src' folder on GitHub!
--
-- Author: 0866
-- License: MIT
-- Version: "1.1.1"
-- GitHub: https://github.com/richie0866/orca
--]]

local pI=require(script.GlobalConfig)
local pJ=require(script.createReconciler)
local pK=require(script.createReconcilerCompat)
local pL=require(script.RobloxRenderer)
local pM=require(script.strict)
local pN=require(script.Binding)
local pO=pJ(pL)
local pP=pK(pO)
local b=pM{
    Component=require(script.Component),
    createElement=require(script.createElement),
    createFragment=require(script.createFragment),
    oneChild=require(script.oneChild),
    PureComponent=require(script.PureComponent),
    None=require(script.None),
    Portal=require(script.Portal),
    createRef=require(script.createRef),
    forwardRef=require(script.forwardRef),
    createBinding=pN.create,
    joinBindings=pN.join,
    createContext=require(script.createContext),
    Change=require(script.PropMarkers.Change),
    Children=require(script.PropMarkers.Children),
    Event=require(script.PropMarkers.Event),
    Ref=require(script.PropMarkers.Ref),
    mount=pO.mountVirtualTree,
    unmount=pO.unmountVirtualTree,
    update=pO.updateVirtualTree,
    reify=pP.reify,
    teardown=pP.teardown,
    reconcile=pP.reconcile,
    setGlobalConfig=pI.set,
    UNSTABLE={}
}
return b
