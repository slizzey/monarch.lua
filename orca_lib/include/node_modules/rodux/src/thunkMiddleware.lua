--[[
-- Orca, a free and open-source Roblox script hub.
-- This script was generated by ci/bundle.lua, and is not intended to be modified.
-- To view the source code, see the 'src' folder on GitHub!
--
-- Author: 0866
-- License: MIT
-- Version: "1.1.1"
-- GitHub: https://github.com/richie0866/orca
--]]

local function uC(oq)
    return debug.traceback(oq)
end
local function up(uL,eu)
    return function(aU)
        if typeof(aU)=="function" then
            local nx,dx=xpcall(function()
                return aU(eu)
            end,uC)
            if not nx then
                eu._errorReporter.reportReducerError(eu:getState(),aU,{message="Caught error in thunk",thrownValue=dx})
                return nil
            end
            return dx
        end
        return uL(aU)
    end
end
return up
