--[[
-- Orca, a free and open-source Roblox script hub.
-- This script was generated by ci/bundle.lua, and is not intended to be modified.
-- To view the source code, see the 'src' folder on GitHub!
--
-- Author: 0866
-- License: MIT
-- Version: "1.1.1"
-- GitHub: https://github.com/richie0866/orca
--]]

local a=_G[script]
local t_=a.import(script,script.Parent,"context").RoactRoduxContext
local h=a.import(script,a.getModule(script,"@rbxts","roact-hooked").out)
local i=h.hooked
local l=h.useMemo
local b=a.import(script,a.getModule(script,"@rbxts","roact").src)
local am=i(function(C)
    local eu=C.store
    local be=C[b.Children]
    local u0=l(function()
        return {store=eu}
    end,{eu})
    local u1={value=u0}
    local u2={}
    local H=#u2
    if be then
        for J,K in pairs(be) do
            if type(J)=="number" then
                u2[H+J]=K
            else
                u2[J]=K
            end
        end
    end
    return b.createElement(t_.Provider,u1,u2)
end)
return {Provider=am}
